<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!--配置别名-->
    <typeAliases>
        <package name="com.itcast.model"></package>
    </typeAliases>

    <!--告诉mybatis加载映射文件-->
    <mappers>
        <mapper resource="com/itcast/sqlmap/User.xml"></mapper>
        <!--第三种：可以写包名-->

        <!-- 包配置只适用于mapper代理
        加载映射接口和配置文件
        -->
        <package name="com.itcast.mapper"></package>

    </mappers>
</configuration>